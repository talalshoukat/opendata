# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=open_data
DB_USER=your_username
DB_PASSWORD=your_password

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4

# Vector Store Configuration
VECTOR_STORE_PATH=./vector_store

# Embedding Configuration
# Options: 'tfidf' (default, uses scikit-learn TF-IDF) or 'openai' (uses OpenAI embeddings)
EMBEDDING_TYPE=tfidf
# OpenAI embedding model (only used when EMBEDDING_TYPE=openai)
# Options: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Embedding Cache Configuration (for OpenAI embeddings)
# Enable/disable caching of embeddings to avoid repeated API calls
EMBEDDING_CACHE_ENABLED=true
# Path where embedding cache will be stored
EMBEDDING_CACHE_PATH=./embedding_cache

# Agent Configuration
MAX_RETRIES=3
MAX_TOOL_CALLS=10

# LLM Configuration
TEMPERATURE=0.1
MAX_TOKENS=4000

# Vector Search Configuration
SIMILARITY_THRESHOLD=0.9
MAX_CANDIDATES=10
